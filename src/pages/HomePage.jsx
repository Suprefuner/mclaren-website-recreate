import { HeroSection, VideoSection, ModelSection } from "../components"

const HomePage = () => {
  return (
    <main>
      <HeroSection />
      <ModelSection />
      <VideoSection />
    </main>
  )
}
export default HomePage
